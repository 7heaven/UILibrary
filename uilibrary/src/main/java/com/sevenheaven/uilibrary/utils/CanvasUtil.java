package com.sevenheaven.uilibrary.utils;

import android.graphics.Canvas;
import android.graphics.Paint;

/**
 * Created by 7heaven on 16/8/9.
 */
public class CanvasUtil {

    public static void drawSmoothLine(Canvas canvas, float[] lines, Paint paint){
        if(canvas != null && paint != null && lines != null && lines.length > 1 && (lines.length & 1) == 0){

        }
    }
}
